# NutriMind Python Dependencies
# ================================
# Tested on Python 3.11 – 3.14
# Install: pip install -r requirements.txt

# IBM Watson / watsonx SDKs
ibm-watsonx-ai>=1.1.2
ibmcloudant>=0.9.0
ibm-cloud-sdk-core>=3.20.0
ibm-cos-sdk>=2.13.0

# MCP server framework (fastmcp v4 + mcp v2 — compatible pair)
fastmcp>=4.0.3
# Note: fastmcp>=4.x pulls mcp>=2.x automatically.
# Do NOT separately pin mcp<2 — use fastmcp's bundled version.

# Web server (pulled by fastmcp, pinned here for explicitness)
uvicorn[standard]>=0.35.0
httpx>=0.27.0

# Vector search
faiss-cpu>=1.12.0

# IBM-native numpy (already a transitive dep; explicit for clarity)
numpy>=1.26.0

# Document processing (optional — for PDF ingestion in rag/ingest.py)
pypdf>=4.0.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0

# IBM Speech to Text (voice input preprocessing — optional)
ibm-watson>=8.1.0
